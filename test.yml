---
version: 1

files:
  - source: README.md
    target: /root/README.md

items:
  - name: Ensure /root/foo exists
    check: ls /root/foo
    action: touch /root/foo
  - name: Ensure sshbox is running
  - name: Check Uptime
    check: false
    action: uptime
